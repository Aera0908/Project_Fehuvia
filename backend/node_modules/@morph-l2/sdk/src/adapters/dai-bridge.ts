/* eslint-disable @typescript-eslint/no-unused-vars */
import { Contract } from 'ethers'

import {
  AddressLike,
} from '../interfaces'
import { toAddress } from '../utils'
import { StandardBridgeAdapter } from './standard-bridge'
import { hexStringEquals } from '../utils/hex-strings'
import { CrossChainMessenger } from '../cross-chain-messenger'
import { getContractInterface } from '../utils/contract-defs'
import { Console } from '../utils/console'

/**
 * Bridge adapter for the DAI bridge.
 */
export class DAIBridgeAdapter extends StandardBridgeAdapter {
  constructor(opts: {
    messenger: CrossChainMessenger
    l1Bridge: AddressLike
    l2Bridge: AddressLike
  }) {
    super(opts)

    this.messenger = opts.messenger

    // let l1BridgeName = 'L1GatewayRouter',
    //   l2BridgeName = 'L2GatewayRouter'
    let l1BridgeName = 'L1DAIGateway',
      l2BridgeName = 'L2DAIGateway'

    Console.log(
      'DAIBridgeAdapter',
      toAddress(
        opts.l1Bridge
      ),
      toAddress(
        opts.l2Bridge
      )
    )
    this.l1Bridge = new Contract(
      toAddress(
        opts.l1Bridge
      ),
      getContractInterface(l1BridgeName),
      this.messenger.l1Provider
    )
    this.l2Bridge = new Contract(
      toAddress(
        opts.l2Bridge
      ),
      getContractInterface(l2BridgeName),
      this.messenger.l2Provider
    )
  }

  public async supportsTokenPair(
    l1Token: AddressLike,
    l2Token: AddressLike
  ): Promise<boolean> {
    // Only support DAI deposits and withdrawals.
    Console.log('supportsTokenPair l1Token', toAddress(l1Token))
    Console.log(
      'supportsTokenPair this.messenger.contracts.l1.DAI.address',
      this.messenger.contracts.l1.DAI?.address
    )
    Console.log('supportsTokenPair l2Token', toAddress(l2Token))
    Console.log('supportsTokenPair L2DAI', this.messenger.contracts.l2?.L2DAI?.address)
    try {
      return (
        hexStringEquals(
          toAddress(l1Token),
          this.messenger.contracts.l1.DAI?.address
        ) && hexStringEquals(toAddress(l2Token), this.messenger.contracts.l2.L2DAI?.address)
      )
    } catch(err) {
      return false
    }
  }
}
