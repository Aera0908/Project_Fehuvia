import { Provider } from '@ethersproject/abstract-provider';
import { BigNumber } from 'ethers';
import { L1Provider, ProviderLike } from './interfaces';
export declare const estimateCrossDomainMessageFee: (l1Provider: ProviderLike, contractAddress: string, contractName?: string) => Promise<BigNumber>;
export declare const isL1Provider: <TProvider extends Provider>(provider: TProvider) => provider is L1Provider<TProvider>;
