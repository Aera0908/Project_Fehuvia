"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getContractInterface = exports.getContractDefinition = exports.getContractArtifact = void 0;
const ethers_1 = require("ethers");
const contractArtifacts = __importStar(require("../abi-configs"));
const abis = __importStar(require("../interfaces/abi-v2"));
const getContractArtifact = (name) => {
    if (contractArtifacts.getContractArtifact) {
        return contractArtifacts.getContractArtifact(name);
    }
    const pathFunc = contractArtifacts[name];
    if (typeof pathFunc !== 'string') {
        return pathFunc;
    }
    else {
        try {
            return require(pathFunc);
        }
        catch (_a) {
            throw new Error(`Unable to getContractArtifact for contract: ${name}`);
        }
    }
};
exports.getContractArtifact = getContractArtifact;
const getContractDefinition = (name, path) => {
    let artifact = (0, exports.getContractArtifact)(name);
    if (!artifact)
        artifact = { abi: abis[name] };
    if (artifact === undefined) {
        throw new Error(`Unable to find artifact for contract: ${name}`);
    }
    return artifact;
};
exports.getContractDefinition = getContractDefinition;
const getContractInterface = (name, path) => {
    const definition = (0, exports.getContractDefinition)(name, path);
    return new ethers_1.ethers.utils.Interface(definition.abi || definition);
};
exports.getContractInterface = getContractInterface;
//# sourceMappingURL=contract-defs.js.map