"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CrossChainMessenger = exports.l2BridgeName = exports.l1BridgeName = exports.l2CrossDomainMessengerName = exports.l1CrossDomainMessengerName = void 0;
const abstract_provider_1 = require("@ethersproject/abstract-provider");
const keccak256_1 = require("@ethersproject/keccak256");
const ethers_1 = require("ethers");
const axios_1 = __importDefault(require("axios"));
const interfaces_1 = require("./interfaces");
const utils_1 = require("./utils");
const misc_1 = require("./utils/misc");
const encoding_1 = require("./utils/encoding");
const hashing_1 = require("./utils/hashing");
const console_1 = require("./utils/console");
exports.l1CrossDomainMessengerName = 'L1CrossDomainMessenger';
exports.l2CrossDomainMessengerName = 'L2CrossDomainMessenger';
exports.l1BridgeName = 'L1GatewayRouter';
exports.l2BridgeName = 'L2GatewayRouter';
class CrossChainMessenger {
    constructor(opts) {
        var _a, _b, _c, _d, _e, _f, _g, _h, _j;
        this.populateTransaction = {
            sendMessage: async (message, opts) => {
                if (message.direction === interfaces_1.MessageDirection.L1_TO_L2) {
                    return this.l1CrossDomainMessenger.populateTransaction.sendMessage(message.target, message.message, await this.estimateL2MessageGasLimit(message), (opts === null || opts === void 0 ? void 0 : opts.overrides) || {});
                }
                else {
                    return this.l2CrossDomainMessenger.populateTransaction.sendMessage(message.target, message.message, 0, (opts === null || opts === void 0 ? void 0 : opts.overrides) || {});
                }
            },
            proveAndRelayMessage: async (message, opts) => {
                console_1.Console.log('proveAndRelayMessage resolved');
                const resolved = await this.toCrossChainMessage(message, opts);
                if (resolved.direction === interfaces_1.MessageDirection.L1_TO_L2) {
                    throw new Error('cannot finalize L1 to L2 message');
                }
                console_1.Console.log('proveAndRelayMessage resolved', resolved);
                const withdrawVerify = await this.getWithdrawMessageProof(resolved);
                console_1.Console.log('proveAndRelayMessage withdrawVerify', withdrawVerify);
                const withdrawal = await this.toLowLevelMessage(resolved);
                console_1.Console.log('proveAndRelayMessage withdrawal', withdrawal);
                const args = [
                    withdrawal.sender,
                    withdrawal.target,
                    withdrawal.value,
                    withdrawal.messageNonce,
                    withdrawal.message,
                    withdrawVerify.withdrawalProof,
                    withdrawVerify.withdrawalRoot,
                ];
                console_1.Console.log('proveAndRelayMessage args', args);
                return this.l1CrossDomainMessenger.populateTransaction['proveAndRelayMessage(address,address,uint256,uint256,bytes,bytes32[32],bytes32)'](...args);
            },
            depositETH: async (amount, opts, isEstimatingGas = false) => {
                const getOpts = async () => {
                    if (isEstimatingGas) {
                        return opts;
                    }
                    const gasEstimation = await this.estimateGas.depositETH(amount, opts);
                    return Object.assign(Object.assign({}, opts), { overrides: {
                            gasLimit: gasEstimation.add(gasEstimation.div(2)),
                        } });
                };
                return this.bridges.ETH.populateTransaction.deposit(ethers_1.ethers.constants.AddressZero, ethers_1.ethers.constants.AddressZero, amount, await getOpts());
            },
            withdrawETH: async (amount, opts, isEstimatingGas) => {
                const getOpts = async () => {
                    if (isEstimatingGas) {
                        return opts;
                    }
                    if (!ethers_1.ethers.Signer.isSigner(this.l2SignerOrProvider)) {
                        throw new Error('unable to deposit withdrawETH an l2 signer');
                    }
                    const from = await this.l2SignerOrProvider.getAddress();
                    const gasEstimation = await this.estimateGas.withdrawETH(amount, Object.assign(Object.assign({}, opts), { overrides: {
                            from,
                        } }));
                    return Object.assign(Object.assign({}, opts), { overrides: {
                            gasLimit: gasEstimation.add(gasEstimation.div(2)),
                            from,
                        } });
                };
                return this.bridges.ETH.populateTransaction.withdraw(ethers_1.ethers.constants.AddressZero, ethers_1.ethers.constants.AddressZero, amount, await getOpts());
            },
            approveERC20: async (l1Token, l2Token, amount, opts) => {
                const bridge = await this.getBridgeForTokenPairFromTokenList(l1Token, l2Token);
                console_1.Console.log('approveERC20 bridge', l1Token, l2Token, amount, opts);
                return bridge.populateTransaction.approve(l1Token, l2Token, amount, opts);
            },
            depositERC20: async (l1Token, l2Token, amount, opts, isEstimatingGas = false) => {
                const bridge = await this.getBridgeForTokenPairFromTokenList(l1Token, l2Token);
                const getOpts = async () => {
                    if (isEstimatingGas) {
                        delete opts.overrides;
                        delete opts.recipient;
                        console_1.Console.log('depositERC20 isEstimatingGas', opts);
                        return opts;
                    }
                    if (!ethers_1.ethers.Signer.isSigner(this.l1SignerOrProvider)) {
                        throw new Error('unable to deposit without an signer');
                    }
                    const gasEstimation = await this.estimateGas.depositERC20(l1Token, l2Token, amount, Object.assign(Object.assign({}, opts), { overrides: {
                            from: opts === null || opts === void 0 ? void 0 : opts.from,
                        } }));
                    return Object.assign(Object.assign({}, opts), { overrides: {
                            gasLimit: gasEstimation.add(gasEstimation.div(2)),
                            from: opts === null || opts === void 0 ? void 0 : opts.from,
                        } });
                };
                return bridge.populateTransaction.deposit(l1Token, l2Token, amount, await getOpts());
            },
            withdrawERC20: async (l1Token, l2Token, amount, opts, isEstimatingGas) => {
                const bridge = await this.getBridgeForTokenPairFromTokenList(l1Token, l2Token);
                const getOpts = async () => {
                    if (isEstimatingGas) {
                        return opts;
                    }
                    if (!ethers_1.ethers.Signer.isSigner(this.l2SignerOrProvider)) {
                        throw new Error('unable to deposit withdrawERC20 an signer');
                    }
                    const gasEstimation = await this.estimateGas.withdrawERC20(l1Token, l2Token, amount, Object.assign(Object.assign({}, opts), { overrides: {
                            from: opts === null || opts === void 0 ? void 0 : opts.from,
                        } }));
                    return Object.assign(Object.assign({}, opts), { overrides: {
                            gasLimit: gasEstimation.add(gasEstimation.div(2)),
                            from: opts === null || opts === void 0 ? void 0 : opts.from,
                        } });
                };
                console_1.Console.log('withdrawERC20 bridge 22', await getOpts());
                return bridge.populateTransaction.withdraw(l1Token, l2Token, amount, await getOpts());
            },
        };
        this.estimateGas = {
            sendMessage: async (message, opts) => {
                const tx = await this.populateTransaction.sendMessage(message, opts);
                if (message.direction === interfaces_1.MessageDirection.L1_TO_L2) {
                    return this.l1Provider.estimateGas(tx);
                }
                else {
                    return this.l2Provider.estimateGas(tx);
                }
            },
            proveAndRelayMessage: async (message, opts) => {
                const estData = await this.populateTransaction.proveAndRelayMessage(message, opts);
                console_1.Console.log('estimateGas proveAndRelayMessage', estData, Object.assign(Object.assign({}, estData), { from: (estData === null || estData === void 0 ? void 0 : estData.from) || (opts === null || opts === void 0 ? void 0 : opts.from) }), opts);
                return this.l1Provider.estimateGas(estData);
            },
            depositETH: async (amount, opts) => {
                const estData = await this.populateTransaction.depositETH(amount, opts, true);
                console_1.Console.log('estimateGas depositETH', estData, opts);
                return this.l1Provider.estimateGas(Object.assign(Object.assign({}, estData), { from: (estData === null || estData === void 0 ? void 0 : estData.from) || (opts === null || opts === void 0 ? void 0 : opts.from) }));
            },
            withdrawETH: async (amount, opts) => {
                const estData = await this.populateTransaction.withdrawETH(amount, opts, true);
                console_1.Console.log('estimateGas withdrawETH', estData);
                return this.l2Provider.estimateGas(Object.assign(Object.assign({}, estData), { from: (estData === null || estData === void 0 ? void 0 : estData.from) || (opts === null || opts === void 0 ? void 0 : opts.from) }));
            },
            approveERC20: async (l1Token, l2Token, amount, opts) => {
                var _a;
                const estData = await this.populateTransaction.approveERC20(l1Token, l2Token, amount, opts);
                console_1.Console.log('estimateGas approveERC20', estData);
                return (((_a = opts === null || opts === void 0 ? void 0 : opts.signer) === null || _a === void 0 ? void 0 : _a.provider) || this.l1Provider).estimateGas(Object.assign(Object.assign({}, estData), { from: estData.from || (opts === null || opts === void 0 ? void 0 : opts.from) }));
            },
            depositERC20: async (l1Token, l2Token, amount, opts) => {
                const estData = await this.populateTransaction.depositERC20(l1Token, l2Token, amount, opts, true);
                console_1.Console.log('estimateGas depositERC20', estData, Object.assign(Object.assign({}, estData), { from: estData.from || (opts === null || opts === void 0 ? void 0 : opts.from) }));
                return this.l1Provider.estimateGas(Object.assign(Object.assign({}, estData), { from: estData.from || (opts === null || opts === void 0 ? void 0 : opts.from) }));
            },
            withdrawERC20: async (l1Token, l2Token, amount, opts) => {
                const estData = await this.populateTransaction.withdrawERC20(l1Token, l2Token, amount, opts, true);
                console_1.Console.log('estimateGas withdrawERC20', Object.assign(Object.assign({}, estData), { from: estData.from || (opts === null || opts === void 0 ? void 0 : opts.from) }));
                return this.l2Provider.estimateGas(Object.assign(Object.assign({}, estData), { from: estData.from || (opts === null || opts === void 0 ? void 0 : opts.from) }));
            },
        };
        this.backendURL = (_a = opts.backendURL) !== null && _a !== void 0 ? _a : 'http://localhost:8080';
        this.l1SignerOrProvider = (0, utils_1.toSignerOrProvider)(opts.l1SignerOrProvider);
        this.l2SignerOrProvider = (0, utils_1.toSignerOrProvider)(opts.l2SignerOrProvider);
        try {
            this.l1ChainId = (0, utils_1.toNumber)(opts.l1ChainId);
        }
        catch (err) {
            throw new Error(`L1 chain ID is missing or invalid: ${opts.l1ChainId}`);
        }
        try {
            this.l2ChainId = (0, utils_1.toNumber)(opts.l2ChainId);
        }
        catch (err) {
            throw new Error(`L2 chain ID is missing or invalid: ${opts.l2ChainId}`);
        }
        this.contracts = (0, utils_1.getAllOEContracts)(this.l2ChainId, {
            l1SignerOrProvider: this.l1SignerOrProvider,
            l2SignerOrProvider: this.l2SignerOrProvider,
            overrides: opts.contracts,
        });
        this.bridges = (0, utils_1.getBridgeAdapters)(this.l2ChainId, this, {
            overrides: opts.bridges,
            contracts: opts.contracts,
        });
        this.l1CrossDomainMessenger = (0, utils_1.getOEContract)(exports.l1CrossDomainMessengerName, this.l2ChainId, {
            address: ((_c = (_b = opts === null || opts === void 0 ? void 0 : opts.contracts) === null || _b === void 0 ? void 0 : _b.l1) === null || _c === void 0 ? void 0 : _c[exports.l1CrossDomainMessengerName]) ||
                ((_e = (_d = utils_1.CONTRACT_ADDRESSES[this.l2ChainId]) === null || _d === void 0 ? void 0 : _d.l1) === null || _e === void 0 ? void 0 : _e[exports.l1CrossDomainMessengerName]),
            signerOrProvider: this.l1SignerOrProvider,
        });
        this.l2CrossDomainMessenger = (0, utils_1.getOEContract)(exports.l2CrossDomainMessengerName, this.l2ChainId, {
            address: ((_g = (_f = opts === null || opts === void 0 ? void 0 : opts.contracts) === null || _f === void 0 ? void 0 : _f.l2) === null || _g === void 0 ? void 0 : _g[exports.l2CrossDomainMessengerName]) ||
                ((_j = (_h = utils_1.CONTRACT_ADDRESSES[this.l2ChainId]) === null || _h === void 0 ? void 0 : _h.l2) === null || _j === void 0 ? void 0 : _j[exports.l2CrossDomainMessengerName]),
            signerOrProvider: this.l2SignerOrProvider,
        });
    }
    get l1Provider() {
        if (abstract_provider_1.Provider.isProvider(this.l1SignerOrProvider)) {
            return this.l1SignerOrProvider;
        }
        else {
            return this.l1SignerOrProvider.provider;
        }
    }
    get l2Provider() {
        if (abstract_provider_1.Provider.isProvider(this.l2SignerOrProvider)) {
            return this.l2SignerOrProvider;
        }
        else {
            return this.l2SignerOrProvider.provider;
        }
    }
    get l1Signer() {
        if (abstract_provider_1.Provider.isProvider(this.l1SignerOrProvider)) {
            throw new Error(`messenger has no L1 signer`);
        }
        else {
            return this.l1SignerOrProvider;
        }
    }
    get l2Signer() {
        if (abstract_provider_1.Provider.isProvider(this.l2SignerOrProvider)) {
            throw new Error(`messenger has no L2 signer`);
        }
        else {
            return this.l2SignerOrProvider;
        }
    }
    async getMessagesByTransaction(transaction, opts = {}) {
        var _a, _b;
        await ((_b = (_a = transaction).wait) === null || _b === void 0 ? void 0 : _b.call(_a));
        const txHash = (0, utils_1.toTransactionHash)(transaction);
        let receipt;
        if (opts.direction !== undefined) {
            if (opts.direction === interfaces_1.MessageDirection.L1_TO_L2) {
                receipt = await this.l1Provider.getTransactionReceipt(txHash);
            }
            else {
                receipt = await this.l2Provider.getTransactionReceipt(txHash);
            }
        }
        else {
            receipt = await this.l1Provider.getTransactionReceipt(txHash);
            if (receipt) {
                opts.direction = interfaces_1.MessageDirection.L1_TO_L2;
            }
            else {
                receipt = await this.l2Provider.getTransactionReceipt(txHash);
                opts.direction = interfaces_1.MessageDirection.L2_TO_L1;
            }
        }
        if (!receipt) {
            throw new Error(`unable to find transaction receipt for ${txHash}`);
        }
        const messenger = opts.direction === interfaces_1.MessageDirection.L1_TO_L2
            ? this.l1CrossDomainMessenger
            : this.l2CrossDomainMessenger;
        return receipt.logs
            .filter((log) => {
            var _a, _b;
            return (((_a = log.address) === null || _a === void 0 ? void 0 : _a.toLocaleLowerCase()) ===
                ((_b = messenger.address) === null || _b === void 0 ? void 0 : _b.toLocaleLowerCase()));
        })
            .filter((log) => {
            const parsed = messenger.interface.parseLog(log);
            return parsed.name === 'SentMessage';
        })
            .map((log) => {
            const parsed = messenger.interface.parseLog(log);
            return {
                direction: opts.direction,
                target: parsed.args.target,
                sender: parsed.args.sender,
                message: parsed.args.message,
                messageNonce: ethers_1.ethers.BigNumber.from(parsed.args.messageNonce),
                value: ethers_1.ethers.BigNumber.from(parsed.args.value),
                minGasLimit: ethers_1.ethers.BigNumber.from(parsed.args.gasLimit),
                logIndex: log.logIndex,
                blockNumber: log.blockNumber,
                transactionHash: log.transactionHash,
            };
        });
    }
    async toLowLevelMessage(message, opts) {
        const resolved = await this.toCrossChainMessage(message, opts);
        if (resolved.direction === interfaces_1.MessageDirection.L1_TO_L2) {
            throw new Error(`can only convert L2 to L1 messages to low level`);
        }
        const encoded = (0, encoding_1.encodeCrossDomainMessageV1)(resolved);
        const withdrawals = await this.getMessagesByTransaction(resolved.transactionHash, {
            direction: interfaces_1.MessageDirection.L2_TO_L1,
        });
        if (withdrawals.length === 0) {
            throw new Error(`no withdrawals found in receipt`);
        }
        if (withdrawals.length > 1) {
            throw new Error(`multiple withdrawals found in receipt`);
        }
        console_1.Console.log('withdrawals', withdrawals);
        const withdrawal = withdrawals[0];
        return Object.assign(Object.assign(Object.assign({}, resolved), withdrawal), { messageSender: this.l2CrossDomainMessenger.address, messageTarget: this.l1CrossDomainMessenger.address, minGasLimit: withdrawal.minGasLimit || resolved.minGasLimit, encodedMessage: encoded, messageHash: (0, keccak256_1.keccak256)(encoded) });
    }
    async getBridgeForTokenPairFromTokenList(l1Token, l2Token) {
        var _a, _b, _c, _d, _e, _f;
        console_1.Console.log('getBridgeForTokenPairFromTokenList', l1Token, l2Token, process.env.MORPH_BRIDGE_TOKEN_LIST);
        const data = await fetch(process.env.MORPH_BRIDGE_TOKEN_LIST).then((response) => {
            if (!response.ok) {
                throw new Error('Network response was not ok');
            }
            return response.json();
        }).then(data => data === null || data === void 0 ? void 0 : data.tokens);
        if (!data) {
            throw new Error('Network response was null');
        }
        console_1.Console.log('getBridgeForTokenPairFromTokenList', data);
        const l1TokenData = data.find(v => {
            var _a;
            return Number(v.chainId) === Number(this.l1ChainId) &&
                l1Token.toLocaleLowerCase() === ((_a = v.address) === null || _a === void 0 ? void 0 : _a.toLocaleLowerCase());
        });
        console_1.Console.log('getBridgeForTokenPairFromTokenList l1TokenData', l1TokenData);
        const l2TokenData = data.find(v => {
            var _a;
            return Number(v.chainId) === Number(this.l2ChainId) &&
                l2Token.toLocaleLowerCase() === ((_a = v.address) === null || _a === void 0 ? void 0 : _a.toLocaleLowerCase());
        });
        console_1.Console.log('getBridgeForTokenPairFromTokenList l2TokenData', l2TokenData);
        if (!l1TokenData && !l2TokenData) {
            console_1.Console.log('getBridgeForTokenPairFromTokenList Custom Token', this.contracts.l1, this.contracts.l2);
            const bridges = await (0, utils_1.getBridgeAdaptersFromTokenList)(this, {
                l1BridgeAddress: (_b = (_a = this.contracts.l1) === null || _a === void 0 ? void 0 : _a['L1CustomERC20Gateway']) === null || _b === void 0 ? void 0 : _b.address,
                l2BridgeAddress: (_d = (_c = this.contracts.l2) === null || _c === void 0 ? void 0 : _c['L2CustomERC20Gateway']) === null || _d === void 0 ? void 0 : _d.address,
            });
            return bridges['Custom'];
        }
        const bridges = await (0, utils_1.getBridgeAdaptersFromTokenList)(this, {
            l1BridgeAddress: l1TokenData.gatewayAddress,
            l2BridgeAddress: l2TokenData.gatewayAddress,
        });
        console_1.Console.log('getBridgeForTokenPairFromTokenList bridges', bridges);
        let bridge = bridges[((_e = l1TokenData === null || l1TokenData === void 0 ? void 0 : l1TokenData.symbol) === null || _e === void 0 ? void 0 : _e.toLocaleUpperCase()) || ((_f = l2TokenData === null || l2TokenData === void 0 ? void 0 : l2TokenData.symbol) === null || _f === void 0 ? void 0 : _f.toLocaleUpperCase())];
        if (bridge) {
            console_1.Console.log('getBridgeForTokenPairFromTokenList ETH / WETH', bridge);
            return bridge;
        }
        else if ((l1TokenData === null || l1TokenData === void 0 ? void 0 : l1TokenData.gatewayName) === 'L1StandardERC20Gateway' || (l2TokenData === null || l2TokenData === void 0 ? void 0 : l2TokenData.gatewayName) === 'L2StandardERC20Gateway') {
            console_1.Console.log('getBridgeForTokenPairFromTokenList StandardERC20', bridges['Standard']);
            return bridges['Standard'];
        }
        else if ((l1TokenData === null || l1TokenData === void 0 ? void 0 : l1TokenData.gatewayName) === 'L1ReverseCustomGateway' || (l2TokenData === null || l2TokenData === void 0 ? void 0 : l2TokenData.gatewayName) === 'L2ReverseCustomGateway') {
            console_1.Console.log('getBridgeForTokenPairFromTokenList StandardERC20', bridges['Standard']);
            return bridges['Morph'];
        }
        else {
            console_1.Console.log('getBridgeForTokenPairFromTokenList Custom Gateway', bridges['Custom']);
            return bridges['Custom'];
        }
    }
    async toCrossChainMessage(message, opts) {
        if (!message) {
            throw new Error('message is undefined');
        }
        if (message.message) {
            return message;
        }
        else if (message.l1Token &&
            message.l2Token &&
            message.transactionHash) {
            const messages = await this.getMessagesByTransaction(message.transactionHash, opts);
            const found = messages
                .sort((a, b) => {
                return a.logIndex - b.logIndex;
            })
                .find((m) => {
                return m.logIndex > message.logIndex;
            });
            if (!found) {
                throw new Error(`could not find SentMessage event for message`);
            }
            console_1.Console.log('toCrossChainMessage found', found);
            return found;
        }
        else {
            const messages = await this.getMessagesByTransaction(message, opts);
            if (messages.length !== 1) {
                throw new Error(`expected 1 message, got ${messages.length}`);
            }
            console_1.Console.log('toCrossChainMessage messages', messages[0]);
            return messages[0];
        }
    }
    async waitRollupSuccess(transactionHash) {
        const msgs = await this.getMessagesByTransaction(transactionHash, {
            direction: interfaces_1.MessageDirection.L2_TO_L1,
        });
        const withdrawNum = msgs[0].blockNumber;
        const tick = Date.now();
        let totalTimeMs = 0;
        while (totalTimeMs < Infinity) {
            const commitNum = await this.getCommittedL2BlockNumber();
            if (Number(commitNum) >= Number(withdrawNum)) {
                console_1.Console.log(`time ${Date.now()} rollup succeed! commit number ${commitNum} , withdraw number ${withdrawNum}`);
                return;
            }
            console_1.Console.log(`time ${Date.now()} wait rollup! commit number ${commitNum} , withdraw number ${withdrawNum}`);
            await (0, misc_1.sleep)(8000);
            totalTimeMs += Date.now() - tick;
        }
    }
    async waitSyncSuccess(transactionHash) {
        const msgs = await this.getMessagesByTransaction(transactionHash, {
            direction: interfaces_1.MessageDirection.L2_TO_L1,
        });
        const withdrawNum = msgs[0].blockNumber;
        let totalTimeMs = 0;
        const tick = Date.now();
        while (totalTimeMs < Infinity) {
            const syncNum = await this.getBackendTreeSyncIndex();
            if (Number(syncNum) >= Number(withdrawNum)) {
                console_1.Console.log(`time ${Date.now()} backend sync succeed! sync number ${syncNum} , need ${withdrawNum}`);
                return;
            }
            console_1.Console.log(`time ${Date.now()} wait backend sync! sync number ${syncNum} , withdraw number ${withdrawNum}`);
            await (0, misc_1.sleep)(2000);
            totalTimeMs += Date.now() - tick;
        }
    }
    async waitBatchFinalize(transactionHash) {
        const msgs = await this.getMessagesByTransaction(transactionHash, {
            direction: interfaces_1.MessageDirection.L2_TO_L1,
        });
        const withdrawNum = msgs[0].blockNumber;
        const tick = Date.now();
        let totalTimeMs = 0;
        while (totalTimeMs < Infinity) {
            const fbi = await this.contracts.l1.Rollup.lastFinalizedBatchIndex();
            const batchStorage = await this.contracts.l1.Rollup.batchDataStore(fbi);
            const finalizeNum = Number(batchStorage.blockNumber);
            if (finalizeNum >= Number(withdrawNum)) {
                console_1.Console.log(`time ${Date.now()} batch finalized succeed! finalize number ${finalizeNum} , withdraw number ${withdrawNum}`);
                return;
            }
            console_1.Console.log(`time ${Date.now()} wait batch finalized! finalize number ${finalizeNum} , withdraw number ${withdrawNum}`);
            await (0, misc_1.sleep)(8000);
            totalTimeMs += Date.now() - tick;
        }
    }
    async getMessageStatus(message, opts = {}) {
        var _a;
        const resolved = await this.toCrossChainMessage(message, opts);
        const receipt = await this.getMessageReceipt(resolved, opts);
        if (resolved.direction === interfaces_1.MessageDirection.L1_TO_L2) {
            if (receipt === null) {
                return interfaces_1.MessageStatus.UNCONFIRMED_L1_TO_L2_MESSAGE;
            }
            else {
                if (receipt.receiptStatus === interfaces_1.MessageReceiptStatus.RELAYED_SUCCEEDED) {
                    return interfaces_1.MessageStatus.RELAYED;
                }
                else {
                    return interfaces_1.MessageStatus.FAILED_L1_TO_L2_MESSAGE;
                }
            }
        }
        else {
            if (receipt === null) {
                const latestL2BlockNum = await this.getCommittedL2BlockNumber();
                console_1.Console.log('getMessageStatus waitRollupSuccess', latestL2BlockNum, resolved.blockNumber, latestL2BlockNum === null || latestL2BlockNum === void 0 ? void 0 : latestL2BlockNum.toString(), resolved.blockNumber.toString(), Number(latestL2BlockNum) < Number(resolved.blockNumber));
                if (Number(latestL2BlockNum) < Number(resolved.blockNumber)) {
                    return interfaces_1.MessageStatus.WITHDRAWAL_ROOT_NOT_PUBLISHED;
                }
                const syncIndex = await this.getBackendTreeSyncIndex();
                console_1.Console.log('getMessageStatus waitSyncSuccess', syncIndex, resolved.blockNumber, syncIndex === null || syncIndex === void 0 ? void 0 : syncIndex.toString(), resolved.blockNumber.toString(), Number(syncIndex) < Number(resolved.blockNumber));
                if (Number(syncIndex) < Number(resolved.blockNumber)) {
                    return interfaces_1.MessageStatus.WITHDRAWAL_HASH_NOT_SYNC;
                }
                const lastFinalizedBatchIndex = await this.contracts.l1.Rollup.lastFinalizedBatchIndex();
                const batchStorage = await this.contracts.l1.Rollup.batchDataStore(lastFinalizedBatchIndex);
                const finalizeNum = (_a = batchStorage === null || batchStorage === void 0 ? void 0 : batchStorage.blockNumber) === null || _a === void 0 ? void 0 : _a.toString();
                console_1.Console.log('waitBatchFinalize waitBatchFinalize', finalizeNum, resolved.blockNumber, finalizeNum === null || finalizeNum === void 0 ? void 0 : finalizeNum.toString(), resolved.blockNumber.toString(), Number(finalizeNum) < Number(resolved.blockNumber));
                if (finalizeNum) {
                    if (Number(finalizeNum) < Number(resolved.blockNumber)) {
                        return interfaces_1.MessageStatus.IN_CHALLENGE_PERIOD;
                    }
                    else {
                        return interfaces_1.MessageStatus.READY_FOR_RELAY;
                    }
                }
                else {
                    const finalizePeriod = await this.contracts.l1.Rollup.FINALIZATION_PERIOD_SECONDS();
                    const withdrawal = await this.toLowLevelMessage(resolved, opts);
                    const provenWithdrawal = await this.getProvenWithdrawal(withdrawal.messageHash);
                    if (provenWithdrawal.timestamp.eq(ethers_1.BigNumber.from(0))) {
                        return interfaces_1.MessageStatus.READY_TO_PROVE;
                    }
                    const latestBlock = await this.l1Provider.getBlock('latest');
                    const withdrawalRootBatchIndex = await this.contracts.l1.Rollup.withdrawalRoots(provenWithdrawal.withdrawalRoot);
                    if (withdrawalRootBatchIndex.gt(lastFinalizedBatchIndex) ||
                        latestBlock.timestamp <
                            provenWithdrawal.timestamp.toNumber() + finalizePeriod.toNumber()) {
                        return interfaces_1.MessageStatus.IN_CHALLENGE_PERIOD;
                    }
                    else {
                        return interfaces_1.MessageStatus.READY_FOR_RELAY;
                    }
                }
            }
            else {
                if (receipt.receiptStatus === interfaces_1.MessageReceiptStatus.RELAYED_SUCCEEDED) {
                    return interfaces_1.MessageStatus.RELAYED;
                }
                else {
                    return interfaces_1.MessageStatus.READY_FOR_RELAY;
                }
            }
        }
    }
    async getMessageReceipt(message, opts = {}) {
        const resolved = await this.toCrossChainMessage(message, opts);
        const messageHash = (0, hashing_1.hashCrossDomainMessagev1)(resolved);
        const messenger = resolved.direction === interfaces_1.MessageDirection.L1_TO_L2
            ? this.l2CrossDomainMessenger
            : this.l1CrossDomainMessenger;
        let isMessageExecuted;
        if (resolved.direction === interfaces_1.MessageDirection.L1_TO_L2) {
            isMessageExecuted = await this.l2CrossDomainMessenger.isL1MessageExecuted(messageHash);
        }
        else {
            isMessageExecuted =
                await this.l1CrossDomainMessenger.finalizedWithdrawals(messageHash);
        }
        console_1.Console.log('isMessageExecuted', isMessageExecuted);
        if (isMessageExecuted) {
            return {
                receiptStatus: interfaces_1.MessageReceiptStatus.RELAYED_SUCCEEDED,
            };
        }
        else {
            return null;
        }
    }
    async waitForMessageReceipt(message, opts = {}) {
        const resolved = await this.toCrossChainMessage(message);
        let totalTimeMs = 0;
        while (totalTimeMs < (opts.timeoutMs || Infinity)) {
            const tick = Date.now();
            const receipt = await this.getMessageReceipt(resolved);
            if (receipt !== null) {
                return receipt;
            }
            else {
                await (0, misc_1.sleep)(opts.pollIntervalMs || 4000);
                totalTimeMs += Date.now() - tick;
            }
        }
        throw new Error(`timed out waiting for message receipt`);
    }
    async waitForMessageStatus(message, status, opts = {}) {
        const resolved = await this.toCrossChainMessage(message, opts);
        let totalTimeMs = 0;
        while (totalTimeMs < (opts.timeoutMs || Infinity)) {
            const tick = Date.now();
            const currentStatus = await this.getMessageStatus(resolved, opts);
            console_1.Console.log('waitForMessageStatus', currentStatus);
            if (resolved.direction === interfaces_1.MessageDirection.L1_TO_L2) {
                if (currentStatus === status) {
                    return;
                }
                if (status === interfaces_1.MessageStatus.UNCONFIRMED_L1_TO_L2_MESSAGE &&
                    currentStatus > status) {
                    return;
                }
                if (status === interfaces_1.MessageStatus.FAILED_L1_TO_L2_MESSAGE &&
                    currentStatus === interfaces_1.MessageStatus.RELAYED) {
                    throw new Error(`incompatible message status, expected FAILED_L1_TO_L2_MESSAGE got RELAYED`);
                }
                if (status === interfaces_1.MessageStatus.RELAYED &&
                    currentStatus === interfaces_1.MessageStatus.FAILED_L1_TO_L2_MESSAGE) {
                    throw new Error(`incompatible message status, expected RELAYED got FAILED_L1_TO_L2_MESSAGE`);
                }
            }
            if (resolved.direction === interfaces_1.MessageDirection.L2_TO_L1) {
                if (currentStatus >= status) {
                    return;
                }
            }
            await (0, misc_1.sleep)(opts.pollIntervalMs || 4000);
            totalTimeMs += Date.now() - tick;
        }
        throw new Error(`timed out waiting for message status change`);
    }
    async estimateL2MessageGasLimit(message, opts) {
        let resolved;
        let from;
        if (message.messageNonce === undefined) {
            resolved = message;
            from = opts === null || opts === void 0 ? void 0 : opts.from;
        }
        else {
            resolved = await this.toCrossChainMessage(message);
            from = (opts === null || opts === void 0 ? void 0 : opts.from) || resolved.sender;
        }
        if (resolved.direction === interfaces_1.MessageDirection.L2_TO_L1) {
            throw new Error(`cannot estimate gas limit for L2 => L1 message`);
        }
        const estimate = await this.l2Provider.estimateGas({
            from,
            to: resolved.target,
            data: resolved.message,
        });
        const bufferPercent = (opts === null || opts === void 0 ? void 0 : opts.bufferPercent) || 20;
        return estimate.mul(100 + bufferPercent).div(100);
    }
    async getProvenWithdrawal(withdrawalHash) {
        return this.l1CrossDomainMessenger.provenWithdrawals(withdrawalHash);
    }
    encodeFunctionMessage(options) {
        const { sender, target, value, messageNonce, message, } = options;
        const encoded = this.l2CrossDomainMessenger.interface.encodeFunctionData('relayMessage', [sender, target, value, messageNonce, message]);
        return (0, keccak256_1.keccak256)(encoded);
    }
    async getCommittedL2BlockNumber() {
        const fbi = await this.contracts.l1.Rollup.lastCommittedBatchIndex();
        console_1.Console.log('getCommittedL2BlockNumber getCommittedL2BlockNumber', fbi.toString());
        const batchStorage = await this.contracts.l1.Rollup.batchDataStore(fbi);
        console_1.Console.log('getCommittedL2BlockNumber batchDataStore', batchStorage, batchStorage.blockNumber.toString());
        const latestL2BlockNum = batchStorage.blockNumber.toString();
        return latestL2BlockNum;
    }
    async getFinalizedL2BlockNumber() {
        const fbi = await this.contracts.l1.Rollup.lastFinalizedBatchIndex();
        console_1.Console.log('getFinalizedL2BlockNumber getFinalizedL2BlockNumber', fbi.toString());
        const batchStorage = await this.contracts.l1.Rollup.batchDataStore(fbi.toString());
        console_1.Console.log('getFinalizedL2BlockNumber batchDataStore', batchStorage.blockNumber.toString());
        const latestL2BlockNum = batchStorage.blockNumber.toString();
        return latestL2BlockNum;
    }
    async getWithdrawMessageProof(message) {
        const resolved = await this.toCrossChainMessage(message);
        console_1.Console.log('getWithdrawMessageProof resolved', resolved);
        if (resolved.direction === interfaces_1.MessageDirection.L1_TO_L2) {
            throw new Error(`can only generate proofs for L2 to L1 messages`);
        }
        console_1.Console.log('getWithdrawMessageProof this.contracts.l1.Rollup', this.contracts.l1.Rollup);
        const latestL2BlockNum = await this.getFinalizedL2BlockNumber();
        console_1.Console.log('getWithdrawMessageProof latestL2BlockNum', latestL2BlockNum);
        console_1.Console.log('getWithdrawMessageProof resolved.blockNumber', resolved.blockNumber);
        if (latestL2BlockNum < resolved.blockNumber) {
            throw new Error(`state root for message not yet published`);
        }
        const withdrawal = await this.toLowLevelMessage(resolved);
        console_1.Console.log('getWithdrawMessageProof withdrawal', withdrawal);
        let withdrawalIndex;
        let withdrawalProof;
        let withdrawalRoot;
        let withdrawalLeaf;
        console_1.Console.log('getWithdrawMessageProof backendURL', this.backendURL + '/getProof');
        await axios_1.default
            .get(this.backendURL + '/getProof', {
            params: {
                nonce: withdrawal.messageNonce,
            },
        })
            .then(async (response) => {
            withdrawalIndex = response.data.index;
            withdrawalProof = response.data.proof;
            withdrawalRoot = response.data.root;
            withdrawalLeaf = response.data.leaf;
            if (withdrawalLeaf !== withdrawal.messageHash) {
                throw new Error(`can not get proof for ${resolved}, leaf not equal expect ${withdrawal.messageHash} got ${withdrawalLeaf}`);
            }
        })
            .catch((error) => {
            throw new Error(`can not get proof from ${this.backendURL}, ${error}`);
        });
        console_1.Console.log('getWithdrawMessageProof reault', {
            withdrawalIndex,
            withdrawalProof,
            withdrawalRoot,
            withdrawalLeaf,
        });
        return {
            withdrawalIndex,
            withdrawalProof,
            withdrawalRoot,
            withdrawalLeaf,
        };
    }
    async getBackendTreeSyncIndex() {
        return await axios_1.default
            .get(this.backendURL + '/getL2ConfirmHeight', {})
            .then((response) => {
            console_1.Console.log('getBackendTreeSyncIndex', response.data);
            return response.data;
        })
            .catch((error) => {
            return 0;
        });
    }
    async sendMessage(message, opts) {
        const tx = await this.populateTransaction.sendMessage(message, opts);
        if (message.direction === interfaces_1.MessageDirection.L1_TO_L2) {
            return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l1Signer).sendTransaction(tx);
        }
        else {
            return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l2Signer).sendTransaction(tx);
        }
    }
    async proveAndRelayMessage(message, opts) {
        console_1.Console.log('proveAndRelayMessage 2');
        const estData = await this.populateTransaction.proveAndRelayMessage(message, opts);
        console_1.Console.log('sendTransaction proveAndRelayMessage 2', estData, opts);
        return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l1Signer).sendTransaction(estData);
    }
    async depositETH(amount, opts) {
        const options = await this.populateTransaction.depositETH(amount, opts);
        return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l1Signer).sendTransaction(options);
    }
    async withdrawETH(amount, opts) {
        const options = await this.populateTransaction.withdrawETH(amount, opts);
        return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l2Signer).sendTransaction(options);
    }
    async approval(l1Token, l2Token, opts) {
        const bridge = await this.getBridgeForTokenPairFromTokenList(l1Token, l2Token);
        console_1.Console.log('approval bridge', bridge);
        return bridge.approval(l1Token, l2Token, opts);
    }
    async approveERC20(l1Token, l2Token, amount, opts) {
        return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l1Signer).sendTransaction(await this.populateTransaction.approveERC20(l1Token, l2Token, amount, Object.assign({ signer: (opts === null || opts === void 0 ? void 0 : opts.signer) || this.l1Signer }, opts)));
    }
    async depositERC20(l1Token, l2Token, amount, opts) {
        console_1.Console.log('depositERC20', '---------------');
        return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l1Signer).sendTransaction(await this.populateTransaction.depositERC20(l1Token, l2Token, amount, opts));
    }
    async withdrawERC20(l1Token, l2Token, amount, opts) {
        const data = await this.populateTransaction.withdrawERC20(l1Token, l2Token, amount, opts);
        console_1.Console.log('withdrawERC20 data', data);
        return ((opts === null || opts === void 0 ? void 0 : opts.signer) || this.l2Signer).sendTransaction(data);
    }
}
exports.CrossChainMessenger = CrossChainMessenger;
//# sourceMappingURL=cross-chain-messenger.js.map