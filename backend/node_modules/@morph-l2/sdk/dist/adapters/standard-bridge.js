"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StandardBridgeAdapter = void 0;
const ethers_1 = require("ethers");
const interfaces_1 = require("../interfaces");
const utils_1 = require("../utils");
const console_1 = require("../utils/console");
const hex_strings_1 = require("../utils/hex-strings");
const contract_defs_1 = require("../utils/contract-defs");
const l1_provider_1 = require("../l1-provider");
class StandardBridgeAdapter {
    constructor(opts) {
        this.populateTransaction = {
            approve: async (l1Token, l2Token, amount, opts) => {
                let token = new ethers_1.Contract((0, utils_1.toAddress)(l1Token), (0, contract_defs_1.getContractInterface)('MorphStandardERC20'), this.messenger.l1Provider);
                let bridgeAddress = this.l1Bridge.address;
                if ((opts === null || opts === void 0 ? void 0 : opts.direction) === interfaces_1.MessageDirection.L2_TO_L1) {
                    token = new ethers_1.Contract((0, utils_1.toAddress)(l2Token), (0, contract_defs_1.getContractInterface)('L2WETH'), this.messenger.l2Provider);
                    bridgeAddress = this.l2Bridge.address;
                }
                console_1.Console.log('token approve', token.address, bridgeAddress, amount);
                return token.populateTransaction.approve(bridgeAddress, amount);
            },
            deposit: async (l1Token, l2Token, amount, opts) => {
                var _a, _b, _c, _d;
                const gasLimit = BigInt(((_b = (_a = opts === null || opts === void 0 ? void 0 : opts.overrides) === null || _a === void 0 ? void 0 : _a.gasLimit) === null || _b === void 0 ? void 0 : _b.toString()) || 520000);
                const gasFee = await (0, l1_provider_1.estimateCrossDomainMessageFee)(this.messenger.l1Provider, (_d = (_c = this.messenger.contracts.l1) === null || _c === void 0 ? void 0 : _c.L1MessageQueueWithGasPriceOracle) === null || _d === void 0 ? void 0 : _d.address);
                console.log('estimateCrossDomainMessageFee gasFee.mul(gasLimit).toBigInt()', gasFee.toString(), gasLimit.toString(), gasFee.mul(gasLimit).toBigInt());
                const options = {
                    value: gasFee.mul(gasLimit).toBigInt(),
                };
                console_1.Console.log(`depositERC20 9`, (0, utils_1.toAddress)(l1Token), (0, utils_1.toAddress)(opts.recipient || opts.from), amount, gasLimit, options);
                if ((opts === null || opts === void 0 ? void 0 : opts.recipient) === undefined) {
                    return this.l1Bridge.populateTransaction['depositERC20(address,uint256,uint256)']((0, utils_1.toAddress)(l1Token), amount, gasLimit, options);
                }
                else {
                    return this.l1Bridge.populateTransaction['depositERC20(address,address,uint256,uint256)']((0, utils_1.toAddress)(l1Token), (0, utils_1.toAddress)(opts.recipient || opts.from), amount, gasLimit, options);
                }
            },
            withdraw: async (l1Token, l2Token, amount, opts) => {
                var _a, _b;
                const gasLimit = BigInt(((_b = (_a = opts === null || opts === void 0 ? void 0 : opts.overrides) === null || _a === void 0 ? void 0 : _a.gasLimit) === null || _b === void 0 ? void 0 : _b.toString()) || 200000);
                const options = {
                    gasLimit,
                };
                console_1.Console.log('withdraw gasLimit', gasLimit, options);
                if ((opts === null || opts === void 0 ? void 0 : opts.recipient) === undefined) {
                    return this.l2Bridge.populateTransaction['withdrawERC20(address,uint256,uint256)']((0, utils_1.toAddress)(l2Token), amount, gasLimit);
                }
                else {
                    return this.l2Bridge.populateTransaction['withdrawERC20(address,address,uint256,uint256)']((0, utils_1.toAddress)(l2Token), (0, utils_1.toAddress)(opts.recipient || opts.from), amount, gasLimit);
                }
            },
        };
        this.estimateGas = {
            approve: async (l1Token, l2Token, amount, opts) => {
                return this.messenger.l1Provider.estimateGas(await this.populateTransaction.approve(l1Token, l2Token, amount, opts));
            },
            deposit: async (l1Token, l2Token, amount, opts) => {
                return this.messenger.l1Provider.estimateGas(await this.populateTransaction.deposit(l1Token, l2Token, amount, opts));
            },
            withdraw: async (l1Token, l2Token, amount, opts) => {
                return this.messenger.l2Provider.estimateGas(await this.populateTransaction.withdraw(l1Token, l2Token, amount, opts));
            },
        };
        this.messenger = opts.messenger;
        let l1BridgeName = 'L1StandardERC20Gateway', l2BridgeName = 'L2StandardERC20Gateway';
        console_1.Console.log('this.messenger.contracts.l1?.[l1BridgeName]', l1BridgeName);
        this.l1Bridge = new ethers_1.Contract((0, utils_1.toAddress)(opts.l1Bridge), (0, contract_defs_1.getContractInterface)(l1BridgeName), this.messenger.l1Provider);
        this.l2Bridge = new ethers_1.Contract((0, utils_1.toAddress)(opts.l2Bridge), (0, contract_defs_1.getContractInterface)(l2BridgeName), this.messenger.l2Provider);
    }
    async supportsTokenPair(l1Token, l2Token) {
        var _a, _b, _c, _d, _e, _f, _g, _h;
        try {
            console_1.Console.log('supportsTokenPair tokens', (0, utils_1.toAddress)(l1Token), (0, utils_1.toAddress)(l2Token));
            if ((0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l1Token), ethers_1.ethers.constants.AddressZero) ||
                (0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l2Token), ethers_1.ethers.constants.AddressZero)) {
                return false;
            }
            if ((0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l1Token), (_a = this.messenger.contracts.l1.WETH) === null || _a === void 0 ? void 0 : _a.address) &&
                (0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l2Token), (_b = this.messenger.contracts.l2.L2WETH) === null || _b === void 0 ? void 0 : _b.address)) {
                return false;
            }
            if ((0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l1Token), (_c = this.messenger.contracts.l1.USDC) === null || _c === void 0 ? void 0 : _c.address) &&
                (0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l2Token), (_d = this.messenger.contracts.l2.L2USDC) === null || _d === void 0 ? void 0 : _d.address)) {
                return false;
            }
            if ((0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l1Token), (_e = this.messenger.contracts.l1.DAI) === null || _e === void 0 ? void 0 : _e.address) &&
                (0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l2Token), (_f = this.messenger.contracts.l2.L2DAI) === null || _f === void 0 ? void 0 : _f.address)) {
                return false;
            }
            return true;
        }
        catch (err) {
            console_1.Console.error('err 123', err);
            if (!((_g = err === null || err === void 0 ? void 0 : err.message) === null || _g === void 0 ? void 0 : _g.toString().includes('CALL_EXCEPTION')) &&
                !((_h = err === null || err === void 0 ? void 0 : err.stack) === null || _h === void 0 ? void 0 : _h.toString().includes('execution reverted'))) {
                console.error('Unexpected error when checking bridge', err);
            }
            return false;
        }
    }
    async approval(l1Token, l2Token, opts) {
        let token = new ethers_1.Contract((0, utils_1.toAddress)(l1Token), (0, contract_defs_1.getContractInterface)('MorphStandardERC20'), this.messenger.l1Provider);
        let bridgeAddress = this.l1Bridge.address;
        let signer = this.messenger.l1Signer;
        console_1.Console.log('approval opts', opts, interfaces_1.MessageDirection.L2_TO_L1);
        if ((opts === null || opts === void 0 ? void 0 : opts.direction) === interfaces_1.MessageDirection.L2_TO_L1) {
            token = new ethers_1.Contract((0, utils_1.toAddress)(l2Token), (0, contract_defs_1.getContractInterface)('L2WETH'), this.messenger.l2Provider);
            bridgeAddress = this.l2Bridge.address;
            signer = this.messenger.l2Signer;
        }
        console_1.Console.log('await signer.getAddress()', token, await signer.getAddress(), bridgeAddress);
        return await token.allowance(await signer.getAddress(), bridgeAddress);
    }
    async approve(l1Token, l2Token, amount, signer, opts) {
        return signer.sendTransaction(await this.populateTransaction.approve(l1Token, l2Token, amount, opts));
    }
    async deposit(l1Token, l2Token, amount, signer, opts) {
        return signer.sendTransaction(await this.populateTransaction.deposit(l1Token, l2Token, amount, opts));
    }
    async withdraw(l1Token, l2Token, amount, signer, opts) {
        return signer.sendTransaction(await this.populateTransaction.withdraw(l1Token, l2Token, amount, opts));
    }
}
exports.StandardBridgeAdapter = StandardBridgeAdapter;
//# sourceMappingURL=standard-bridge.js.map