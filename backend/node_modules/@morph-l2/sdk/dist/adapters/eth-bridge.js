"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ETHBridgeAdapter = void 0;
const ethers_1 = require("ethers");
const utils_1 = require("../utils");
const standard_bridge_1 = require("./standard-bridge");
const hex_strings_1 = require("../utils/hex-strings");
const console_1 = require("../utils/console");
const l1_provider_1 = require("../l1-provider");
const contract_defs_1 = require("../utils/contract-defs");
class ETHBridgeAdapter extends standard_bridge_1.StandardBridgeAdapter {
    constructor(opts) {
        super(opts);
        this.populateTransaction = {
            approve: async (l1Token, l2Token, amount, opts) => {
                throw new Error(`approvals not necessary for ETH bridge`);
            },
            deposit: async (l1Token, l2Token, amount, opts) => {
                var _a, _b, _c, _d;
                if (!(await this.supportsTokenPair(l1Token, l2Token))) {
                    throw new Error(`token pair not supported by bridge`);
                }
                const gasLimit = BigInt(((_b = (_a = opts === null || opts === void 0 ? void 0 : opts.overrides) === null || _a === void 0 ? void 0 : _a.gasLimit) === null || _b === void 0 ? void 0 : _b.toString()) || '2000000');
                const gasFee = await (0, l1_provider_1.estimateCrossDomainMessageFee)(this.messenger.l1Provider, (_d = (_c = this.messenger.contracts.l1) === null || _c === void 0 ? void 0 : _c.L1MessageQueueWithGasPriceOracle) === null || _d === void 0 ? void 0 : _d.address);
                console_1.Console.log('estimateCrossDomainMessageFee gasFee.mul(gasLimit).toBigInt()', gasFee.toString(), gasLimit.toString(), gasFee.mul(gasLimit).toBigInt());
                const options = {
                    value: BigInt(amount) + gasFee.mul(gasLimit).toBigInt(),
                };
                console_1.Console.log(`depositing this.l1Bridge ETH`, this.l1Bridge, gasFee, gasLimit);
                if ((opts === null || opts === void 0 ? void 0 : opts.recipient) === undefined) {
                    return this.l1Bridge.populateTransaction['depositETH(uint256,uint256)'](amount, gasLimit, options);
                }
                else {
                    return this.l1Bridge.populateTransaction['depositETH(address,uint256,uint256)']((0, utils_1.toAddress)(opts.recipient || opts.from), amount, gasLimit, options);
                }
            },
            withdraw: async (l1Token, l2Token, amount, opts) => {
                var _a;
                if (!(await this.supportsTokenPair(l1Token, l2Token))) {
                    throw new Error(`token pair not supported by bridge`);
                }
                const gasLimit = BigInt((((_a = opts === null || opts === void 0 ? void 0 : opts.overrides) === null || _a === void 0 ? void 0 : _a.gasLimit) || '300000').toString());
                const options = {
                    value: BigInt(amount),
                    gasLimit,
                };
                console_1.Console.log(`withdraw ${amount} ETH`, gasLimit, options);
                if ((opts === null || opts === void 0 ? void 0 : opts.recipient) === undefined) {
                    return this.l2Bridge.populateTransaction['withdrawETH(uint256,uint256)'](amount, gasLimit, options);
                }
                else {
                    return this.l2Bridge.populateTransaction['withdrawETH(address,uint256,uint256)']((0, utils_1.toAddress)(opts.recipient || opts.from), amount, gasLimit, options);
                }
            },
        };
        this.messenger = opts.messenger;
        let l1BridgeName = 'L1GatewayRouter', l2BridgeName = 'L2GatewayRouter';
        console_1.Console.log('ETHBridgeAdapter', (0, utils_1.toAddress)(opts.l1Bridge), (0, utils_1.toAddress)(opts.l2Bridge));
        this.l1Bridge = new ethers_1.Contract((0, utils_1.toAddress)(opts.l1Bridge), (0, contract_defs_1.getContractInterface)(l1BridgeName), this.messenger.l1Provider);
        this.l2Bridge = new ethers_1.Contract((0, utils_1.toAddress)(opts.l2Bridge), (0, contract_defs_1.getContractInterface)(l2BridgeName), this.messenger.l2Provider);
    }
    async approval(l1Token, l2Token, opts) {
        throw new Error(`approval not necessary for ETH bridge`);
    }
    async supportsTokenPair(l1Token, l2Token) {
        return ((0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l1Token), ethers_1.ethers.constants.AddressZero) &&
            (0, hex_strings_1.hexStringEquals)((0, utils_1.toAddress)(l2Token), ethers_1.ethers.constants.AddressZero));
    }
}
exports.ETHBridgeAdapter = ETHBridgeAdapter;
//# sourceMappingURL=eth-bridge.js.map